{
	"name": "create_external_view_cycles",
	"properties": {
		"content": {
			"query": "CREATE OR ALTER VIEW dbo.cycles_delta_view AS\nSELECT * \nFROM OPENROWSET(\n    BULK 'datalake/gold/cycles_delta/',\n    DATA_SOURCE = 'bess_datalake',\n    FORMAT = 'DELTA'\n) AS rows;",
			"metadata": {
				"language": "sql"
			},
			"currentConnection": {
				"databaseName": "MyDB",
				"poolName": "Built-in"
			},
			"resultLimit": 5000
		},
		"type": "SqlQuery"
	}
}